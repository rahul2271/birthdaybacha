export default function TestPage() {
  return (
    <div style={{ padding: "4rem", fontFamily: "sans-serif", color: "black", background: "white", minHeight: "100vh" }}>
      <h1>Test page — if this stays visible, base Next.js works fine.</h1>
      <p>Go back to <a href="/">the birthday page</a> to compare.</p>
    </div>
  );
}
